export default function Footer(props: { className?: string }) {
    return (
        <footer className={`${props.className} bg-zinc-800 p-4 text-center text-lg text-white`}>
            DH2642-VT23 Group 4
        </footer>
    );
}
